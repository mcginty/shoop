[package]
name = "shoop"
version = "0.0.1-prealpha.1"
authors = ["Jake McGinty <me@jake.su>"]
description = "Shoop is a high-speed encrypted file transfer tool reminiscent of scp. It uses SSH to bootstrap authentication and encryption, then uses UDT (a reliable protocol from the 2000s) instead of TCP (a reliable protocol from the 1970s)."
keywords = ["scp", "transfer", "encrypted", "crypto", "secure", "file transfer", "network"]
homepage = "https://github.com/mcginty/shoop"
repository = "https://github.com/mcginty/shoop"
readme = "README.md"
license = "MIT"

[lib]
name = "shoop"
path = "src/lib.rs"

[[bin]]
name = "shoop"
path = "src/main.rs"

[dependencies]
log = "*"
env_logger = "*"
# rust-crypto = "^0.2"
getopts = "0.2.14"
daemonize = "*"
byteorder = "^0.5"
# rand = "0.3"
sodiumoxide = "0.0.12"
rustc-serialize = "^0.3"
time = "^0.1"
# serde = "^0.7"
#mio = "0.5"
#mioco = "*"
#resolve = "*"

[dependencies.udt]
git = "https://github.com/eminence/udt-rs"

